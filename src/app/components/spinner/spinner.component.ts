import {Component} from '@angular/core';

@Component({
  selector: 'mat-spinner',
  templateUrl: 'spinner.component.html',
  styleUrls: ['spinner.component.css'],
})
export class spinnerComponent {
    data : '<mat-spinner></mat-spinner>';
}